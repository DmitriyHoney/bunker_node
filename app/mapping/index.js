const { PlayerInfoMap, PlayerInfoMapLite } = require('./player');
const { RoomInfoMap } = require('./room');
module.exports = {
    player: {
        PlayerInfoMap,
        PlayerInfoMapLite
    },
    room: {
        RoomInfoMap
    }
}
